
# GitHub APK Build

The `.github/workflows/android.yml` workflow builds the debug APK automatically
when `main` or `master` is pushed, and can also be started manually from GitHub
Actions. The resulting APK is uploaded as the `SG-Technical-debug-apk` artifact.
